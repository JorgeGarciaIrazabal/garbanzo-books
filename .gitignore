# Python
.venv/
__pycache__/
*.pyc

# Build output (generated — never hand-edit; regenerate with build_site.py)
# /site/ is the studio preview build (with drafts) AND the CI deploy target.
/site/
# /site_publish/ is the studio's "what GitHub Pages will see" preview (published only).
# Generated by the studio's Publish button or `make build-publish`.
/site_publish/

# Node / UI
ui/node_modules/
tests/frontend/node_modules/
npm-debug.log*

# pytest / vitest caches
.pytest_cache/
.ruff_cache/
tests/frontend/coverage/

# Env / secrets
.env
*.local

# OS
.DS_Store
